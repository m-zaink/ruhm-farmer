# `components` directory 

This directory shall hold files containing components specific to a given screen.
This shall make the code in the screens file more modular and also avoid
duplication of code.

